spring:
  application:
    name: socialmedia
  datasource:
    url: jdbc:mysql://localhost:3307/social_media
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true